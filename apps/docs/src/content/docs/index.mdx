---
title: Admin design system
description: An opinionated design system for admin interfaces.
template: splash
hero:
  tagline: An opinionated design system for admin interfaces.
  actions:
    - text: Browse components
      link: components/buttons/
      icon: right-arrow
      variant: primary
---

import { Card, CardGrid, LinkCard } from "@astrojs/starlight/components";

<CardGrid>
  <Card title="Built for admin tooling" icon="rocket">
    Density-first defaults for screens an operator sits in all day. Predictable primitives, minimal chrome.
  </Card>
  <Card title="Modern web platform" icon="star">
    Disclosure uses `<details>` and `popover`, modals use `<dialog>`, animation is CSS transitions, state is read with `:has()` and `:user-valid`. Less JavaScript to ship.
  </Card>
  <Card title="Two flavors, one source of truth" icon="puzzle">
    Vanilla CSS for any stack, React components for typed call sites. Both render the same class names.
  </Card>
  <Card title="Not for marketing surfaces" icon="warning">
    Use a different system for customer-facing or marketing surfaces.
  </Card>
</CardGrid>

## Pick a flavor

<CardGrid>
  <LinkCard
    title="Vanilla CSS"
    href={`${import.meta.env.BASE_URL}getting-started/vanilla/`}
    description="A pre-built stylesheet. Drop it in via <link> — no build setup, no Tailwind required."
  />
  <LinkCard
    title="React"
    href={`${import.meta.env.BASE_URL}getting-started/react/`}
    description="A tree-shakeable component library. Same class names as the CSS package."
  />
</CardGrid>

On npm: [`@aortl/admin-css`](https://www.npmjs.com/package/@aortl/admin-css) · [`@aortl/admin-react`](https://www.npmjs.com/package/@aortl/admin-react).
