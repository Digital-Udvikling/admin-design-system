---
title: Textareas
description: Base class .textarea + a variant + an optional size. Identical DOM from either package.
---

import { Textarea } from "@aortl/admin-react";
import Example from "../../../../components/Example.astro";
import { TextareaFieldDemo } from "../../../../demos/TextareaFieldDemo";

## Examples

### Variants

<Example
  html={`<textarea class="textarea textarea-bordered" placeholder="Bordered (default)"></textarea>
<textarea class="textarea textarea-ghost" placeholder="Ghost"></textarea>
<textarea class="textarea textarea-danger" placeholder="Danger">invalid</textarea>`}
  react={`<Textarea placeholder="Bordered (default)" />
<Textarea variant="ghost" placeholder="Ghost" />
<Textarea variant="danger" defaultValue="invalid" />`}
>
  <div
    style={{
      display: "flex",
      flexDirection: "column",
      gap: "0.75rem",
      width: "100%",
      maxWidth: "24rem",
    }}
  >
    <Textarea placeholder="Bordered (default)" />
    <Textarea variant="ghost" placeholder="Ghost" />
    <Textarea variant="danger" defaultValue="invalid" />
  </div>
</Example>

### Sizes

<Example
  html={`<textarea class="textarea textarea-bordered textarea-sm" placeholder="Small"></textarea>
<textarea class="textarea textarea-bordered" placeholder="Medium"></textarea>
<textarea class="textarea textarea-bordered textarea-lg" placeholder="Large"></textarea>`}
  react={`<Textarea textareaSize="sm" placeholder="Small" />
<Textarea placeholder="Medium" />
<Textarea textareaSize="lg" placeholder="Large" />`}
>
  <div
    style={{
      display: "flex",
      flexDirection: "column",
      gap: "0.75rem",
      width: "100%",
      maxWidth: "24rem",
    }}
  >
    <Textarea textareaSize="sm" placeholder="Small" />
    <Textarea placeholder="Medium" />
    <Textarea textareaSize="lg" placeholder="Large" />
  </div>
</Example>

### Disabled

<Example
  html={`<textarea class="textarea textarea-bordered" disabled>Disabled</textarea>`}
  react={`<Textarea disabled defaultValue="Disabled" />`}
>
  <div style={{ width: "100%", maxWidth: "24rem" }}>
    <Textarea disabled defaultValue="Disabled" />
  </div>
</Example>

### Inside a Field

Wrap a `Textarea` in `Field` to get a label, description, and validation messaging — the same Base UI wiring used by `Input`. The invalid border tint flows in automatically when validation fails.

<Example
  html={`<div class="field">
  <label class="field-label" for="notes">Notes</label>
  <textarea id="notes" class="textarea textarea-bordered" placeholder="Anything you'd like to share"></textarea>
  <p class="field-description">Markdown is supported.</p>
</div>`}
  react={`<Field name="notes">
  <Field.Label>Notes</Field.Label>
  <Textarea placeholder="Anything you'd like to share" />
  <Field.Description>Markdown is supported.</Field.Description>
</Field>`}
>
  <div style={{ width: "100%", maxWidth: "24rem" }}>
    <TextareaFieldDemo client:load />
  </div>
</Example>
