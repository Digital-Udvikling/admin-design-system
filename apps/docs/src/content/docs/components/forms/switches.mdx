---
title: Switches
description: Base class .switch with an inner .switch-thumb. React wraps Base UI; vanilla styles a native input.
---

import { Field, Switch } from "@aortl/admin-react";

A toggle for on/off state. Use a switch when the change applies immediately (e.g. enable a feature). For form-submission booleans, prefer a [checkbox](../checkboxes/).

## Examples

### Default

:::example

```html
<input type="checkbox" role="switch" class="switch" />
<input type="checkbox" role="switch" class="switch" checked />
```

```tsx
<Switch />
<Switch defaultChecked />
```

:::

### Disabled

:::example

```html
<input type="checkbox" role="switch" class="switch" disabled />
<input type="checkbox" role="switch" class="switch" disabled checked />
```

```tsx
<Switch disabled />
<Switch disabled defaultChecked />
```

:::

### Inside a Field

:::example

Add `.field-row` to lay the switch and its label out on one line. The default `.field` stacks vertically.

```html
<div class="field field-row">
  <input type="checkbox" role="switch" class="switch" checked />
  <label class="field-label">Email notifications</label>
</div>
```

```tsx
<Field name="notify" className="field-row">
  <Switch defaultChecked />
  <Field.Label>Email notifications</Field.Label>
</Field>
```

:::
